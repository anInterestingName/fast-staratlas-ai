# AGENTS.md

本文件适用于仓库根目录及全部子目录，供参与 fast-staratlas-ai 开发的 AI 代理和工程师使用。交流、文档、代码注释和提交说明默认使用中文，代码标识符保持英文。规范与实际代码不一致时，以用户当前要求、已确认需求和当前代码事实为准。

## 1. 项目定位与事实来源

fast-staratlas-ai 是基于 Python 3.12、FastAPI、Pydantic Settings 和 uv 的单应用 HTTP 服务。工程由官方 `fastapi-new` 脚手架生成，并按 FastAPI Bigger Applications 约定拆分路由、配置和模型。当前为可扩展骨架：业务接口位于 `app/api/routes/`，请求/响应模型位于 `app/schemas/`，配置位于 `app/core/config.py`，自动化测试位于 `tests/`。

遇到文档、配置和现有代码不一致时，按以下优先级判断：

1. 用户当前需求、已确认需求文档和验收标准。
2. `pyproject.toml`、`.python-version`、`.env.example`、SQL/迁移脚本（如已引入）和当前代码。
3. 目标路由、Schema、配置项及实际运行行为。
4. 同类型已完成模块的实现模式。
5. `README.md` 中的背景说明。

发现已确认文档与代码不一致时，不得静默选择对实现最方便的一方。先核对变更历史和关联文档；能够确认实际状态时同步修正文档，无法确认且影响范围或验收时记录为开放问题并向用户说明。

## 2. 工程结构与边界

```text
app/
  main.py                 # FastAPI 应用入口
  core/                   # 配置、统一异常处理与日志
  db/                     # PostgreSQL 引擎与 Session
  api/router.py           # 路由汇总
  api/routes/             # 业务与基础设施路由
  schemas/                # Pydantic 请求/响应模型
  llm/                    # LLM 配置提供者、模型工厂与单步编排
tests/                    # pytest + TestClient
doc/
  requirements/           # 需求文档（首次建档时创建）
  design/                 # 详细设计
  database/               # 数据库设计
  test/                   # 测试文档与执行归档
  templates/              # 文档模板
  sql/                    # 全量与升级 SQL/迁移脚本（引入持久化后维护）
  requirements-index.md   # 需求索引
.env.example              # 环境变量示例，不含真实密钥
```

- `app/main.py` 只负责创建应用、根路径和挂载路由，不堆积业务逻辑。
- 业务路由放在 `app/api/routes/`，通过 `app/api/router.py` 汇总；业务 API 默认使用 `settings.api_v1_prefix`（当前为 `/api/v1`），`/health`、`/ready` 等基础设施路由除外。
- 对外契约使用 `app/schemas/` 中的 Pydantic 模型；路由函数使用类型注解和 `response_model`，不返回未建模的临时字典，除非该接口本身就是稳定的简单结构（如根路径欢迎信息）。
- 配置通过 `app/core/config.py` 的 `Settings` 读取，环境变量示例只维护 `.env.example`；真实 `.env` 不得提交。
- PostgreSQL 已作为数据库产品接入连接层，`/ready` 探测连通性；当前无业务表。新增表、缓存、认证或外部客户端前，必须先完成对应需求/设计，不得把临时存储、密钥或连接信息写进仓库。
- 数据库结构变化必须同步 `doc/sql` 中的全量与升级脚本，不得只修改临时 SQL 或仅改模型定义。

## 3. 开始开发前

1. 查看 `git status`，保留用户已有改动，不修改无关文件和生成物。
2. 读取 `doc/requirements-index.md` 及目标需求、详细设计和测试文档；没有关联文档时判断任务是否属于需要建档的需求驱动变更。
3. 同时检查目标路由、Schema、配置、测试和（如已存在）持久化/客户端边界，不以单个路径或表面需求推断完整范围。
4. 检查认证、配置、数据存储、缓存、外部依赖、兼容性和调用方影响。
5. 优先复用现有 FastAPI 路由拆分、Pydantic 模型和 pytest 模式；新增依赖、目录层或公共抽象前说明必要性。

## 4. 实现契约

- 新增 Python 文件使用 UTF-8，补充与现有风格一致的模块/函数类型注解；公开函数和路由保持简短说明，不写无信息量注释。
- 路由使用 `APIRouter`，声明 `prefix`、`tags`、状态码和 `response_model`；错误使用 `HTTPException` 或项目统一异常处理，响应结构保持稳定。
- 请求体、查询参数和响应模型放在 `app/schemas/`，用 `Field` 约束长度、必填和示例；不要在路由里散落重复校验。
- 配置项加入 `Settings` 和 `.env.example`，使用类型和默认值；禁止把真实密码、Token、密钥、连接串写入代码或文档。
- 写操作明确数据变化边界；重复请求、并发更新和失败补偿按需求决定幂等键、唯一约束、锁或版本校验。当前无业务表，不得把未立项的内存结构当作生产存储。
- 外部 HTTP 调用必须有超时、明确失败结果和调用方处理策略，禁止把降级或空结果误判为正常业务数据。
- 日志使用结构化或占位方式携带必要业务标识，不记录密码、Token、密钥、连接串和完整敏感请求体。
- 测试放在 `tests/`，使用 `TestClient` 覆盖正常、缺失资源和校验失败路径；需要隔离的可变状态必须提供可重复的重置入口。

### 4.1 审计、脱敏等可选横切能力

- 审计、脱敏等横切能力默认不属于基础业务功能。只有需求文档、验收标准或用户当前要求明确提出时，才可纳入设计、数据模型、接口、配置、实现和测试范围。
- 未明确要求时，不得主动增加审计表、审计接口、审计中间件、脱敏字段、脱敏规则、权限项、配置项、相关依赖或验收条件，也不得将其作为基础功能完成的前置条件。
- 基础功能应通过清晰的模块边界和稳定契约保持后续扩展能力，但不得为尚未确认的横切能力预先实现抽象层、扩展点、占位字段或空配置。
- 禁止记录密码、Token、密钥、连接串和完整敏感请求体属于通用安全底线，不等同于建设脱敏功能。未要求脱敏时，只需避免采集、持久化和输出不必要的敏感数据，不新增脱敏处理流程。
- 文档中对未要求的审计、脱敏能力应标记为“不涉及”并简述依据，不得写成默认方案、待办事项、开放问题或隐含验收条件。
- 后续新增审计或脱敏要求时，应作为明确的需求变更或独立需求处理，重新评估接口、数据库、权限、配置、发布、回滚和测试影响。

总原则：审计、脱敏等横切能力无明确需求则不设计、不实现、不预留；基础功能仅保持边界开放，并遵守不采集、不记录、不泄露敏感信息的通用安全底线。

## 5. 文档契约

### 5.1 文档体系与模板

- `doc/requirements-index.md` 是需求编号、版本、状态和关联文档的统一登记入口。
- 需求文档使用 `doc/templates/requirements-template.md`。
- 详细设计使用 `doc/templates/detailed-design-template.md`。
- 数据库设计使用 `doc/templates/database-design-template.md`。
- 测试文档使用 `doc/templates/test-document-template.md`。
- 新文档沿用 `REQ-YYYY-NNN` 编号：
  - 需求：`doc/requirements/REQ-YYYY-NNN-short-name.md`
  - 设计：`doc/design/DESIGN-REQ-YYYY-NNN-short-name.md`
  - 数据库：`doc/database/DB-REQ-YYYY-NNN-short-name.md`
  - 测试：`doc/test/TEST-REQ-YYYY-NNN-short-name.md`
- `short-name` 使用简短英文 kebab-case。同一需求的关联文档必须使用相同编号和 `short-name`。
- 模板用于保证信息完整，不得用占位符覆盖已经确认的业务事实；未知信息填写“待指定”“待准备”或“待执行”，不得猜测。填写时按本项目技术栈替换模板中的示例，不引入本仓库不存在的框架概念。

### 5.2 适用范围与建档时机

以下任务默认属于需求驱动任务，应维护完整文档链：

- 新增业务能力、模块、HTTP 接口、数据表、持久化或重要配置。
- 改变对外行为、认证/权限边界、业务状态、数据语义或兼容策略。
- 跨模块、数据库迁移、外部依赖、发布顺序或回滚风险较高的变更。
- 用户明确要求需求、设计、测试或验收文档。

纯拼写修正、注释修正、无行为变化的小型维护可不新建需求编号，但不得借此规避对实际行为变化的记录。无法判断时，先根据改动范围形成文档草稿或向用户确认。

### 5.3 需求驱动流程

需求讨论、设计、开发、测试和验收是同一条文档链，状态变化时统一维护，不得只修改代码或其中一份文档：

1. 需求范围明确后，先在索引分配编号并立即递增“下一个编号”。
2. 从需求模板创建需求文档，明确目标、非目标、范围、流程、业务规则、安全边界和可执行验收标准。
3. 需求达到可评审状态后，从详细设计模板创建设计文档，定义模块边界、HTTP 契约、事务/状态、配置、测试、发布和回滚方案。
4. 涉及数据库变化时，从数据库设计模板创建独立文档，定义表、字段、约束、索引、历史数据、升级和回滚；不涉及则在需求、设计和索引中写明“不涉及”。
5. 数据库设计确认后，同步 `doc/sql` 中的全量与升级脚本，详细设计只保留数据设计摘要和链接。
6. 实现过程中若实际方案偏离设计，先评估需求和兼容性影响，再同步关联文档、风险和变更记录，禁止仅让代码成为唯一事实来源。
7. 开发完成或详细设计将测试列为待办时，从测试模板创建测试文档，并从验收标准、业务规则和设计风险生成覆盖矩阵与用例。
8. 测试执行后记录逐用例结果、证据、缺陷、环境限制和清理结果，再同步需求、设计、数据库设计和索引。
9. 只有全部必要验收标准和回归项通过，且不存在未关闭的阻断或严重缺陷时，需求才能标记为“已验收”。

### 5.4 版本、状态与关联链接

- 文档版本从 `0.1` 开始；评审确认后使用 `1.0`，确认后的兼容变化递增次版本，重大范围变化递增主版本。
- 每次版本变化必须更新最后更新日期和变更记录。
- 索引状态只使用：待编写、待评审、已确认、设计中、开发中、已验收、已废弃。
- 仅完成代码或部分验证时保持“开发中”，可在正文写“开发完成，待验收”。
- 需求、详细设计、数据库设计和测试文档应互相关联；索引必须登记全部已有文档。
- 文档新增、重命名或关联变化后，检查所有相对链接，避免单向关联、编号不一致或失效路径。

### 5.5 需求文档编写要求

- 需求只描述业务意图、规则、边界和可观察结果，不提前锁死无必要的类名、SQL 或内部实现。
- 每项 Must 需求至少有一个稳定编号和一个 Given/When/Then 风格的验收标准。
- 明确正常、异常、重复、边界和依赖失败流程，以及数据是否改变。
- 涉及登录、角色或数据权限时，写清身份、作用域、资源归属和拒绝行为。当前仓库尚未实现认证时，写明是否需要登录以及未实现项为何不在范围内。
- 明确 HTTP API、数据库、环境变量/配置、部署和调用方是否受影响；不适用项给出简短依据。
- 需求确认后的范围变化必须记录原因、影响和是否需要重新评审。

### 5.6 详细设计编写要求

- 设计必须映射关联需求和验收标准，说明具体影响的路由、Schema、配置和测试。
- 对 HTTP API 记录方法、路径、参数位置、Pydantic 请求/响应模型、状态码、错误语义、幂等和兼容策略。
- 对外部调用记录目标、超时、失败处理和发布顺序；不涉及则写“不涉及”。
- 对数据记录存储方式、一致性、缓存和数据库设计链接；字段、索引及迁移细节放入数据库设计。未落库的能力必须标明非持久化。
- 对配置记录 Settings 字段、环境变量、默认值、敏感性和发布方式；敏感值不得写入文档。
- 对事务或状态记录本地事务/写入边界、并发控制、重复请求和失败补偿。
- 对发布记录数据库、配置、API 契约和应用部署的先后顺序及可执行回滚方案。
- 实现完成后记录实际完成范围、设计偏差、已执行验证、未执行项、环境限制和待验收项。

### 5.7 数据库设计编写要求

- 仓库已接入 PostgreSQL 连接层，但只有需求明确需要新表或结构变化时才创建/更新数据库设计；否则在需求、详细设计和索引中写明“不涉及”。无表变更时不要写空 DDL。
- 引入数据库时必须先确认数据库产品，不得默认套用其他项目的表前缀、雪花主键或租户字段。
- 必须包含 ER 图、表关系、字段、约束、索引、全量脚本、升级脚本、迁移顺序、回滚和验证。
- ER 实体框、字段清单和 DDL 列顺序应保持一致：`id` 固定在首位，其后依次放置核心业务字段，再放租户或技术字段（如有）。
- 唯一键、并发、历史数据回填和删除后的业务键复用必须有明确方案，不能仅依赖应用层口头约束。
- 变更必须同步 `doc/sql` 中的全量与升级脚本；禁止只提交模型定义或临时 SQL。

### 5.8 测试文档编写要求

需求进入“开发完成，待验收”、用户明确要求测试文档，或详细设计将测试列为交付物时，必须在 `doc/test/` 创建或更新关联测试文档，不得另建只有用例列表或结论的简化文件。

1. 保留模板中的文档信息、范围、依据、环境、准备、用例、覆盖矩阵、缺陷、汇总、清理和变更记录。
2. 从需求逐条提取验收标准、业务规则、异常流程、安全约束、权限边界和回归要求，每项验收标准至少对应一个用例。
3. 每个用例至少包含唯一编号、优先级、关联需求、测试层级、目标模块与入口、依赖、前置条件、数据、完整步骤、可观察预期、证据、清理、实际结果和状态。
4. HTTP 用例明确状态码、响应结构和数据副作用；外部调用用例明确超时/失败和调用方处理；SQL 用例明确目标数据库、升级、重复执行和回滚检查。
5. 安全用例按需覆盖未登录、无权限、敏感字段和日志泄露。
6. 写操作定义唯一数据前缀、允许范围和精确清理步骤。禁止自动删除未知归属、生产或历史数据。
7. 用例结果只使用“未执行”“通过”“失败”“阻塞”“不适用”，汇总数量必须与实际用例一致。
8. 只有实际执行并取得证据的检查才能写入结果；静态检查、pytest、接口联调和业务验收不可互相替代。

### 5.9 测试结果归档

- 测试完成或因明确阻塞阶段性收尾时，记录执行范围、各状态数量、逐用例结果、证据位置、缺陷、数据清理和环境限制。
- 未执行、阻塞、不适用、重试后不稳定和仍需真实环境验证的内容必须保留，不得为了形成通过结论而删除。
- 存在失败、阻塞或必要用例未执行时，需求保持“开发中”或原未完成状态。
- 测试文档、日志摘要和缺陷记录不得包含密码、Token、密钥、连接串或可还原的敏感业务数据。
- 归档后检查实际 diff、文档链接、用例编号、覆盖矩阵和汇总数字，并同步索引及关联文档。

## 6. 命令与验证

当前仓库使用 Python 3.12 和 uv，不使用 Maven 或 JDK。依赖以 `pyproject.toml` / `uv.lock` 为准。

```bash
uv sync --group dev
uv run ruff check
uv run ruff format --check
uv run pytest
uv run fastapi dev
```

- 代码行为变更后，优先运行受影响的 pytest；需要时可运行全部测试。Python 源码变更还需使 Ruff 检查与格式化检查通过。
- AI 可以运行 `uv sync --group dev`、`uv run ruff check`、`uv run ruff format --check` 和 `uv run pytest`。未经用户要求，不启动长期运行的 `fastapi dev`/`uvicorn`，不连接真实外部服务、生产数据库或未授权接口。
- 只记录实际执行并取得结果的命令；pytest 通过不等于配置、权限、持久化或业务验收通过。
- 文档变更至少检查 Markdown 结构、编号一致性、相对链接、模板占位符和索引状态，不需要为纯文档变更运行 pytest。

## 7. 变更与提交纪律

- 保持改动聚焦，不覆盖用户未提交的修改，不顺手格式化范围外文件。
- 不手工编辑 `__pycache__/`、`.venv/`、`.pytest_cache/`、IDE 配置和其他生成物，除非用户明确要求。
- 修改公共 API、认证、配置项、环境变量或数据库脚本前，必须核对兼容性和发布影响。
- 不在仓库中写入真实密码、Token、私钥、数据源凭据、本机专用地址或生产数据。
- Git 提交使用项目既有风格的中文说明，仅在用户明确要求时提交。

## 8. 完成标准

交付前确认：

- 实现与现有 FastAPI 分层和风格一致，路由、Schema、配置和测试边界正确。
- 认证、权限、数据存储、事务/写入边界、缓存和错误路径按需求处理；未要求的能力不擅自引入。
- 数据库和配置变化包含兼容、发布与回滚说明。
- 已运行与风险匹配且获准执行的验证，没有虚构结果。
- 没有无关变更、调试代码、生成物或敏感信息。
- 需求驱动任务已按第 5 节同步需求、详细设计、数据库设计、测试和索引。

最终说明改动文件、行为或文档变化、已执行的验证，以及仍需在真实环境完成的测试和验收。
